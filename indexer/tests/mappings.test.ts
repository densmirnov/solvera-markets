import { test, assert, clearStore } from "matchstick-as/assembly/index";

// Placeholder test to be expanded with generated types and event mocks.
// Ensures test harness is wired for indexer mappings.

test("indexer test harness placeholder", () => {
  assert.booleanEquals(true, true);
  clearStore();
});
